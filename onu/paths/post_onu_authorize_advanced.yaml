tags:
- ONUs
summary: Authorize ONU Advanced
description: description pending....
operationId: addAuthorizeOnuAdvanced
requestBody:
  content:
    application/json:
      schema:
        $ref: '#/components/requestBodies/AuthorizeOnuAdvancedRequestPOST'
responses:
  '200':
    description: Created
    content:
      application/json:
        schema:
          # add

x-code-samples:
  - lang: Python
    source: |
      import requests
      endpoint = "https://{{subdomain}}.adminolt.com/api/onu/authorize/"
  
      headers = {
          "Authorization": "Token {{token}}" 
      }
      
      data = {
      }
      
      response = requests.post(url=endpoint, headers=headers, data=data)
      print(response.content)
security:
  - api_key: []