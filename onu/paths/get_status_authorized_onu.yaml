tags:
- ONUs
summary: Get status of authorized onu
description: Get status of authorized onu trough of websocket with key-> facility
operationId: getStatusOfuthorizedOnu
parameters:
  - name: id_onu
    in: path
    description: id_onu of ONU to return
    required: true
    schema:
      type: integer
      format: number
responses:
  '200':
    description: Successful Operation
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/GetStatusAuthorizedOnu'
  '203':
    description: Non-Authoritative Information
    content:
      application/json:
        example:
          message_status: "El token enviado no es valido"      
  '401':
    description: Unauthorized
    content:
      application/json:
        example:
          detail: "Las credenciales de autenticación no se proveyeron."
  '405':
    description: Method Not Allowed
    content:
      application/json:
        example:
          detail: "Metodo {{put,post,delete}} no permitido"
x-code-samples:
  - lang: Python
    source: |
      import requests
      endpoint = "https://{{subdomain}}.adminolt.com/api/info-onu-authorized/run-status-onu/<int:id_onu>/"
      headers = {
          "Authorization": "Token {{token}}" 
      }
      response = requests.get(url=endpoint, headers=headers)
      print(response.content)