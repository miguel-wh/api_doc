tags:
- ONUs
summary: Authorize ONU
description: description pending....
operationId: addAuthorizeONU
requestBody:
  content:
    application/json:
      schema:
        $ref: '#/components/requestBodies/AuthorizeOnuRequestPOST'

info:
  description: CONFIGURACION AVANZADA
  required: true
responses:
  '200':
    description: Created
    content:
      application/json:
        schema:
          # add

x-code-samples:
  - lang: Python
    source: |
      import requests
      endpoint = "https://{{subdomain}}.adminolt.com/api/onu/authorize/"
  
      headers = {
          "Authorization": "Token {{token}}" 
      }
      
      data = {
      }
      
      response = requests.post(url=endpoint, headers=headers, data=data)
      print(response.content)
